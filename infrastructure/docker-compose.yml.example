version: "3"

services:
  ihatetobudget:
    build: .
    command: pipenv run uvicorn ihatetobudget.asgi:application --host 0.0.0.0 --port 8000
    restart: always
    ports:
      - '8000:80'
    volumes:
      - ./infrastructure/persist:/usr/src/app/data
    env_file: docker-compose.env
